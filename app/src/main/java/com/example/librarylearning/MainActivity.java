package com.example.librarylearning;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        //create new-> new module -> Android library (named= LearningLibrary)->
        // create new class in (LearningLibrary-java-com.example.learninglibrary {named "LibraryMessage"} -> create a toast there)
    }
}